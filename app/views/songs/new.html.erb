<div class="max-w-xl mx-auto px-4 py-6">
  <h1 class="text-2xl font-bold mb-4">New Song</h1>

  <%= render "form", song: @song %>

  <%= link_to "← Back to Playlist", playlist_path(@playlist), class: "text-blue-600 hover:underline mt-4 block" %>
</div>
