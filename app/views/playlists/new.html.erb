<div class="max-w-xl mx-auto px-4 py-6">
  <h1 class="text-2xl font-bold mb-4">New Playlist</h1>

  <%= render "form", playlist: @playlist %>

  <%= link_to "← Back to Playlists", playlists_path, class: "text-blue-600 hover:underline mt-4 block" %>
</div>
